import { NgHtmlChangeDirective } from './ng-html-change.directive';

describe('NgHtmlChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgHtmlChangeDirective();
    expect(directive).toBeTruthy();
  });
});
